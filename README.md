The ComBomb repo manifest,
My workflow is as follows:

    repo init -u git@github.com:cdesjardins/ComBombManifest.git
    repo sync
    git checkout -b master origin/master
    git commit -a
    git push
